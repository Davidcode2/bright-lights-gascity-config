scope = "city"
provider = "codex"
wake_mode = "fresh"
work_dir = ".gc/worktrees/hq/polecats/{{.AgentBase}}"
prompt_template = ".gc/system/packs/core/assets/prompts/pool-worker.md"
nudge = "Run gc hook; it checks assigned work first, then routed pool work."
idle_timeout = "2h"
min_active_sessions = 0
max_active_sessions = 3
